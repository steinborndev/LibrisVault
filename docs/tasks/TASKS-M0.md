# TASKS-M0 — Foundation

Goal (SPEC.md §10): a single PDF is ingested correctly (wiki pages + index + hot cache) via a CLI trigger of the service, and both M0 risk probes are documented. No dashboard, no watcher, no queue yet.

Work top to bottom. Check off tasks as they complete. Record findings inline under "Findings".

## 0. Environment sanity check

- [x] Verify WSL2 distro (Ubuntu 24.04 recommended): `lsb_release -a`, `uname -r` — Ubuntu 24.04.3 LTS (noble), kernel 6.6.87.2-microsoft-standard-WSL2 ✅
- [x] Node ≥ 20 via nvm: `node -v`; git present in the distro: `git --version` — git 2.43.0 ✅; nvm 0.40.1 + Node v20.20.2 / npm 10.8.2 installed ✅ (see PATH caveat under Findings)
- [x] Verify current Anthropic policy on Agent SDK use with subscriptions (SPEC.md §7.1 policy caveat): check https://support.claude.com/en/articles/15036540 and note the status under Findings

## 1. Vault setup

- [x] `git clone https://github.com/AgriciDaniel/claude-obsidian ~/vault` (pin: record the cloned commit/tag under Findings) — cloned, pinned to tag `v1.9.2` on branch `vault-main`
- [x] `cd ~/vault && bash bin/setup-vault.sh` — ran clean; created `.raw/`, `wiki/{concepts,entities,sources,meta}`, `_templates/`, wrote `.obsidian/{graph,app,appearance}.json`, downloaded Excalidraw `main.js` (~8 MB, gitignored)
- [x] Confirm Generic mode is active (`.vault-meta/mode.json` absent or `generic`); do NOT run `setup-mode.sh` — `mode.json` absent ⇒ Generic ✅; `setup-mode.sh` NOT run
- [x] Initialize/verify git in the vault; make a baseline commit — git came with the clone; baseline commit `7c0acde` ("chore: baseline after setup-vault.sh"), tree clean
- [x] ~~Open the vault in Obsidian for Windows via `\\wsl$\<distro>\home\<user>\vault`~~ — **impossible, see risk probe A.** Superseded by the decision below: Obsidian runs *inside* WSL via WSLg, opening `~/vault` as a local path. Task rewritten accordingly:
- [x] Open the vault in **Obsidian for Linux inside WSL (WSLg)** at `~/vault` — works. Install note: the `.deb` (1.12.7) does not declare `libasound2` in `Depends`, so the binary fails at launch with `libasound.so.2: cannot open shared object file`. Fix: `sudo apt install libasound2t64` (Ubuntu 24.04 renamed the package for the 64-bit `time_t` transition — plain `libasound2` has no install candidate). Worth putting in the setup script for M5.

### Risk probe A — Obsidian over \\wsl$

- [x] With the seeded vault open: measure app start, graph view open, note open/edit latency; then copy ~200 dummy .md files in and re-check — **not measurable: Obsidian never loads the vault at all** (hard error, not slowness). No latency numbers exist to collect; the 200-file step was moot.
- [x] Verdict under Findings: acceptable / borderline / unusable. If unusable, evaluate fallback `/mnt/d/vault` incl. a `wiki-lock.sh` locking test on drvfs (SPEC.md §3, §11.1) — **verdict: UNUSABLE**. Fallback evaluated; `wiki-lock.sh` passes all tests on drvfs. **Decision needed — see Findings.**

## 2. Authentication

- [x] Install Claude Code CLI in WSL if absent — installed via `npm i -g @anthropic-ai/claude-code` under nvm (**no sudo needed**), version 2.1.197 ✅
- [x] run `claude setup-token` — done by the user (interactive browser OAuth flow)
- [x] Store token as `CLAUDE_CODE_OAUTH_TOKEN` in `~/.config/vault-service/env` (chmod 600) — done; 108-char token, dir `chmod 700`, file `chmod 600`. ⚠️ The first token was leaked and had to be regenerated — see Findings.
- [x] ensure `ANTHROPIC_API_KEY` is NOT set in the environment — verified unset, no shell rc references it. Additionally hardened: the agent subprocess environment is built with exactly one credential (see Findings).
- [x] Smoke test: minimal Agent SDK script (`query()` with a trivial prompt) runs and returns a result — `server/src/cli/smoke.ts`, `npm run smoke --workspace server`. **Passes: replies "pong", 2 tokens in / 4 out.** It found a real bug first — see Findings.

## 3. Service repo scaffold

- [x] Initialize this repo per the layout in CLAUDE.md (server/, web/ placeholder, docs/tasks/, scripts/) — git initialized; layout created; `CLAUDE.md`/`SPEC.md` moved `docs/` → repo root to match CLAUDE.md's own documented layout; commit `c4ae101`
- [x] TypeScript strict + ESM config, vitest, eslint; `npm test` runs (empty suite passes) — TS strict (+`noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `verbatimModuleSyntax`), ESM/NodeNext, npm workspaces. `npm test` **19 passed**, `npm run typecheck` and `npm run lint` clean (suite is not empty — see next line)
- [x] Config module: loads `VAULT_ROOT`, credentials env, validates with zod; implements the double-credential refusal (CLAUDE.md hard rule 3) — `server/src/config.ts` + 19 unit tests. Validates `VAULT_ROOT` structurally (must contain `wiki/` + `skills/`, catches pointing at the wrong dir before an agent run writes), enforces exactly-one-credential on the **merged** env-file+process view, treats empty values as unset, and exposes `describeConfig()` as the only log-safe view (credential reduced to a length).

## 4. Minimal agent runner + CLI trigger

- [x] Implement `server/src/pipeline/agent-runner.ts`: wraps `@anthropic-ai/claude-agent-sdk` `query()` with `cwd = VAULT_ROOT`, `settingSources: ['project']`, edit auto-accept scoped to `VAULT_ROOT`, bash whitelist (vault `scripts/*.sh`), no web tools — done, **but with a deliberate deviation from the task wording**: see "acceptEdits" note under Findings. Permission logic split into `server/src/pipeline/permissions.ts` so it is unit-testable without spawning an agent.
- [x] System-prompt extension (constant, exported): full-automation rules (no questions; document defaults) + English-language rule (SPEC.md §3.1) — `server/src/pipeline/system-prompt.ts`, exported as `AUTOMATION_SYSTEM_PROMPT`, appended to the `claude_code` preset
- [x] Stream handling: persist SDK messages to stdout for now (job_logs comes in M1); capture usage (tokens) from the result message — via an `onMessage` sink so M1 swaps stdout for `job_logs` at the call site, not inside the runner. Usage counts cache-read/-creation tokens into `tokensIn` (a cached run would otherwise under-report). `cost_usd` is captured but flagged as an estimate in oauth mode (SPEC.md §7.1).
- [x] CLI entry: `npm run ingest -- <path-to-file>` → copies file to `VAULT_ROOT/.raw/m0-test/`, runs `ingest .raw/m0-test/<file>` — `server/src/cli/ingest.ts`; config-guard paths verified live (see Findings)
- [x] 15-minute timeout with clean abort — `DEFAULT_TIMEOUT_MS`, composed with an optional caller `signal` (M1 job cancellation) so timeout and cancellation stay distinguishable in the result

## 5. End-to-end validation

- [x] Pick a real ~10–20 page PDF (mixed English/German content is ideal) and run the CLI ingest — a 25-page, 4 MB open-access review article from a materials-science journal (English). **English-only — see the language-rule caveat below.** Ingested **twice**, and the comparison is the interesting part:
  | | run 1 — improvised | run 2 — via the skill, sandboxed |
  |---|---|---|
  | path | `Skill()` errored → agent read `SKILL.md` and executed it by hand | `Skill({skill: 'claude-obsidian:wiki-ingest'})` |
  | turns | 143 | **55** |
  | duration | 828.9 s | **569.3 s** |
  | tokens in | 12.67 M | **5.41 M** |
  | pages | 13 | **15** |
  | wikilinks | 194, 0 broken | **216, 0 broken** |
  The deterministic path is not just cheaper — it is *more* thorough: it additionally produced four further concept pages and maintained `overview.md`, all of which run 1 missed. Run 1 is preserved on tag `m0-ingest-improvised`; the vault was reset to baseline (user-approved) so run 2 could be validated from scratch.
  **The sandbox costs nothing:** 52 tool calls, 0 errors, not one `Read-only file system` collision — the vault scripts write under `.vault-meta/locks/`, which is inside `allowWrite`.
- [x] Verify in the vault: source page + concept/entity pages created (8–15 expected per repo docs), `wiki/index.md` and `wiki/log.md` updated, `wiki/hot.md` refreshed, all wikilinks resolve in Obsidian — **15 pages** (7 concepts, 7 entities, 1 source), in range. `index.md`, `hot.md`, `log.md`, `overview.md` + the three type sub-indexes all updated. **216 wikilinks across the 15 new pages, 0 broken** (verified against every file type, since Obsidian resolves links to `.canvas`/`.base` too, not just `.md` — an `.md`-only checker reported 36 false positives). The vault's 18 pre-existing broken links all sit in the repo's demo content and predate this run.
- [x] Verify the language rule held: page names/summaries in English despite German source passages — **VERIFIED with a second, all-German source:** a 48-page German consumer information brochure on investment funds. 14 pages, 199 wikilinks, 0 broken, vault commit `046edec`. The rule holds, and demonstrably:
  - **Concept names are English despite the German source:** every concept page carries an English name that is a *translation* of the German term, not a transliteration (a figure of speech for a three-way trade-off became its English equivalent, a fee term became a descriptive English name, a fund type and a banking-regulation term became their standard English terms).
  - **The de/en-duplicate defence works as designed:** every German source term is stored as an **alias** on the English page it maps to. So a future German document mentioning the same term resolves to the existing English page instead of spawning a duplicate: the exact German-name-next-to-English-name failure SPEC.md §3.1 names, prevented and observable.
  - **Proper nouns correctly kept German as entities:** the national financial regulator and the statute it enforces; a specific German statute has no English concept equivalent, so it stays. (My first automated check naively flagged the statute's abbreviation as a violation; it is correct behaviour, the check was wrong, not the ingest.)
  - **Verbatim quotes keep the original + a note:** the source page carries the German title with "(German original)" and an English rendering alongside.
- [x] Commit the ingest result in the vault with message `ingest: <source>` — vault commit `313715e`. Not straightforward: see the auto-commit finding below.

### Risk probe B — ingest-skill interactivity

- [x] Note under Findings: did the run try to ask questions or stall? Did the system-prompt extension suffice, or do we need the thin auto-ingest wrapper skill (SPEC.md §11.2)? If needed, create `scripts/vault-extensions/` with the wrapper and an install script, then re-run the validation — **verdict below; no wrapper skill needed for interactivity, but a separate blocker was found.**

## 6. Milestone close

- [x] All acceptance criteria of SPEC.md §10 M0 met — **yes.** SPEC.md §10's M0 criterion — *"Eine PDF wird per CLI-Aufruf des Services korrekt eingepflegt (Seiten + Index + Hot Cache)"* — met twice (the English review article, the German brochure), both via `npm run ingest`, both with 0 broken links. Both risk probes documented. **Every item on this task list is now ticked**, including 5.3 (the language rule), which was the one item still open at the previous milestone-close attempt and is now verified against a real German source.
- [x] Findings section below completed; open follow-ups converted into TASKS-M1.md entries — `docs/tasks/TASKS-M1.md` created; §0 carries the remaining M0 follow-ups, §1 the four spec corrections M0 evidence demands. (Two of the original §0 items — the language rule and the runtime auto-commit-opt-out check — were completed by the German ingest and are struck there.)
- [x] Tag repo `m0` — annotated tag `m0` created on `a9367ee` (user-approved). Decision: tag the as-is state now; the four owed spec corrections (TASKS-M1 §1) land in M1. Vault `.obsidian` view-state committed too (`ed00472`); both trees clean.

## Findings

- Policy status (task 0.3): **Checked 2026-07-17** against https://support.claude.com/en/articles/15036540. The article leads with a pause notice: "We're pausing the changes to Claude Agent SDK usage described below. For now, nothing has changed: Claude Agent SDK, `claude -p`, and third-party app usage still draw from your subscription's usage limits." The announced separate SDK allowance (Pro $20 / Max 5x $100 / Max 20x $200 per month) remains **not active** since the 15 June pause. Consequence for this project: SPEC.md §7.1 is accurate as written — the OAuth/subscription path (`CLAUDE_CODE_OAUTH_TOKEN`) is viable for M0, and SDK ingest runs compete with interactive Claude usage for the same limits. No spec change needed; re-verify before M5 hardening.
- Environment (task 0.1/0.2): Ubuntu 24.04.3 LTS (noble) on WSL2 kernel 6.6.87.2 ✅ · git 2.43.0 ✅ · Node was absent (apt candidate 18.19.1 is too old for the ≥20 requirement), so **nvm 0.40.1 + Node v20.20.2 / npm 10.8.2 were installed** during this session ✅ · **Claude Code CLI is not installed in this distro** (needed for task 2.1 `claude setup-token`) · no preprocessing tooling yet (`pdftotext`, `pandoc`, `exiftool` all absent — M1 concern, not M0-blocking).
- **nvm/PATH caveat (relevant for M5 systemd):** nvm sources from `~/.zshrc`, so Node is only on PATH in interactive shells. Non-interactive/non-login shells (and therefore a systemd user service) will NOT find `node`. The M5 unit must set an absolute interpreter path or an explicit `Environment=PATH=`; do not rely on nvm's shell hook. Carried into TASKS-M1/M5 as a follow-up.
- Repo state: The repo root contains only `docs/` plus three orphaned `*:Zone.Identifier` files (`CLAUDE.md:Zone.Identifier`, `SPEC.md:Zone.Identifier`, `TASKS-M0.md:Zone.Identifier`) — Windows NTFS alternate-data-stream leftovers from a copy where the actual `.md` files did not land at root. `CLAUDE.md` and `SPEC.md` currently live in `docs/`, while CLAUDE.md's own layout section places them at the repo root. Also: **the repo is not git-initialized yet** (task 3.1 territory). Resolve during task 3.1.
- Vault pin (task 1.1): Cloned from https://github.com/AgriciDaniel/claude-obsidian on 2026-07-17. **Pinned to tag `v1.9.2` = commit `00213b720cdc9bb00ec8b3f88f9cc408721c37f9`** ("release: promote v1.9.2 to public canonical…", 2026-05-28), checked out on local branch `vault-main` (a branch, not detached HEAD, so service ingest commits have a base). `origin` remote kept for deliberate upgrades (SPEC.md §11.4). Note: upstream `main` (`cb93ff6`) is exactly **one** commit ahead of v1.9.2 and that commit only adds a 1280x640 social-preview asset — i.e. pinning costs us nothing functional today. Baseline commit after setup: `7c0acde`. Vault state: 50 MB, 49 `.md` files under `wiki/` (repo ships a seeded/demo wiki incl. `index.md`, `hot.md`, `log.md`).
- Risk probe A verdict: **UNUSABLE — and for a different reason than the spec anticipated.** SPEC.md §11.1 predicted a *performance* risk ("9p file access can be sluggish"). Reality is a **hard functional failure**: Obsidian for Windows refuses to load the vault at all, with `Error: EISDIR: illegal operation on a directory, watch '\\wsl.localhost\Ubuntu\home\benjamin\vault\'`. Obsidian's file watcher (Electron/Node `fs.watch` → `ReadDirectoryChangesW`) cannot watch directories over the 9p share. No latency could be measured because the app never reaches a usable state; the "+200 dummy files" step was therefore moot.
  - **Not fixable by tuning.** Confirmed as a known, won't-fix limitation: the Obsidian forum's WSL-vault thread sits in the **"Bug graveyard"** category. The same 9p watch failure hits other Electron apps (MarkText #3779, Cypress #21530). No workaround exists for Windows-side Obsidian on `\\wsl$`/`\\wsl.localhost`. Sources: https://forum.obsidian.md/t/cant-create-or-open-vault-in-wsl-folder/34688 · https://forum.obsidian.md/t/support-for-vaults-in-windows-subsystem-for-linux-wsl/8580
  - **`/mnt/d` does not exist on this machine.** Windows drives present: C (475 GB free), M (1.4 TB free), T (772 GB free); R and S are ≥98 % full. SPEC.md hardcodes `/mnt/d` twice — §4.2 (watch-folder default `/mnt/d/inbox`) and §11.1 (fallback `/mnt/d/vault`). **Both need a spec correction regardless of which option is chosen below.**
  - **drvfs locking test (`wiki-lock.sh`): PASS — the spec's locking concern is unfounded.** Tested via the script's `WIKI_LOCK_VAULT` override on `/mnt/c` (v9fs/drvfs) against an ext4 baseline. All mechanisms behave identically on both: `flock -x` mutual exclusion (fd-form, as used by `with_meta_lock`), `set -o noclobber` O_EXCL atomicity, acquire/peek/release round-trip, 8-way concurrent acquire (exactly 1 winner, 7× EX_TEMPFAIL), and age-based stale reaping. ⚠️ Method note: a first version of the probe reported flock as broken **on ext4 too** — that was a bug in the probe (mixing flock's fd-form and `-c` command-form), not a filesystem defect. Retested with the exact invocation form `wiki-lock.sh` uses. Lesson: a baseline that fails is a broken test, not a finding.
  - **drvfs performance penalty (measured, 200 small .md files):** writes 12 ms → 402 ms (**33×**), reads 7 ms → 245 ms (**35×**), `grep -rl` 3 ms → 197 ms (**65×**), git init+add+commit+status 102 ms → 2277 ms (**22×**). `find` and `stat` are ~par. So SPEC.md §3's rationale for the WSL filesystem is **half right**: the performance argument is strongly confirmed; the locking-semantics argument is not.
  - **Decision (user, 2026-07-17): Obsidian runs inside WSL via WSLg; the vault stays on ext4 at `~/vault`.** The alternative — vault on `/mnt/c/vault` with the native Windows Obsidian — was rejected because it contradicts SPEC.md §3's placement decision and taxes every agent run and git commit forever (20–65×), to fix a problem that turned out not to be filesystem-related.
  - **FINAL VERDICT: acceptable, with one documented GUI limitation.** Measured with the seeded vault (49 notes) and again after adding 200 dummy notes (249 total, 5× the content):
    - App start, opening notes, switching notes, typing: **fluid at 249 notes** — no degradation from the 5× growth.
    - Graph view: **laggy, and equally laggy before and after the 200 notes were added.**
    - The invariance is the whole finding. A filesystem bottleneck would scale with note count; this does not. `/dev/dri` is absent (no DRM render node) — WSLg virtualizes the GPU via `/dev/dxg`, and Electron falls back to software rasterization. Graph view is the one Obsidian surface running a continuous force-directed animation, so it is the one that shows it. Cause is **rendering, not I/O**. Corroborated by the ext4 benchmark: 200 note writes in 12 ms.
    - **Honest trade-off, not a free win:** graph-view lag is the price of WSLg, and it is not fixable by moving the vault — on `/mnt/c` with native Windows Obsidian the graph would render fast (real GPU) while the service paid 20–65× on every operation. There is no configuration that gives both a fast graph and a fast service; Windows-side Obsidian cannot open a WSL vault at all. We chose service speed and daily editing over graph animation. Revisit only if graph view turns out to be load-bearing in practice.
    - Follow-up for M5 (does not block M0): if the graph becomes a real irritant, try forcing Electron's GPU path (`obsidian --use-gl=…` / `--enable-features=VaapiVideoDecoder`) or installing the Mesa d3d12 driver so WSLg's `/dev/dxg` is actually used. Not attempted — out of M0 scope.
- Risk probe B verdict: **The interactivity risk did NOT materialise — the system-prompt extension is sufficient. No wrapper skill needed on that count.** Evidence from the real run: 143 turns, zero questions, zero stalls, ran to completion unattended in 828.9 s. The agent closed with an explicit **"Automated decisions"** list — exactly the behaviour `AUTOMATION_SYSTEM_PROMPT` asks for — documenting four judgement calls (skill invocation, transport fallback, entity scope, domain organisation). SPEC.md §11.2's mitigation works as designed; **the wrapper skill of SPEC.md §11.2 can stay unbuilt for now.**
  - **BUT the probe surfaced something the spec did not anticipate: the vault's `wiki-ingest` skill was never invoked as a skill.** The agent tried `Skill({skill: "wiki-ingest"})`, got a tool error ("not registered as invocable in this session"), then read `skills/wiki-ingest/SKILL.md` and executed its steps by hand. **The ingest succeeded that way** — but by the agent improvising from the instructions, not by the skill mechanism the whole architecture assumes. SPEC.md §3.1 says `settingSources: ['project']` is what loads the skills; that is **not sufficient**. The SDK has a dedicated `skills` option ("Skills to enable for the main session. **This is the single place to turn skills on**"), which the runner does not set — its bundle turns `skills: 'all'` into `allowedTools: ['Skill']`. Untested hypothesis; must be verified before M1 relies on skill invocation. **Consequence if left unfixed: every ingest is a fresh improvisation from SKILL.md rather than a deterministic skill run — precisely the non-determinism SPEC.md §11.2 was written to avoid.**
  - Secondary find (the agent's own decision #2): `scripts/detect-transport.sh` hangs past its 120 s timeout and never returns. The agent fell back to the filesystem transport and continued. Worth diagnosing before anything depends on that script.
- **BLOCKER — CLAUDE.md hard rule 4 is unenforced in practice; `canUseTool` is never consulted.** Found by reading the ingest log, not by the tests: the agent ran `find ~/vault …`, `ls`, `cat`, `python3 …` and `echo … && …` chains — all of which `decidePermission` refuses — and every one succeeded (135 tool-ok, 0 denials). Verified empirically with a deny-everything probe (`server/src/cli/permprobe.ts`): **`canUseTool` invocations: 0; the canary command ran.** The unit tests were green because they test `decidePermission` in isolation and can never observe that the SDK does not call it.
  - **Root cause, from the SDK's own warning text:** *"Bare allowedTools entries auto-approve the whole tool before the callback is consulted. **To gate every tool call, use a PreToolUse hook**; … Allow rules from settings files can also shadow the callback but are not visible here."* So `canUseTool` is advisory and shadowable by design, not a security boundary. (The vault ships no `.claude/settings.json`, so the precise shadow source here is still unidentified — the fix does not depend on knowing it.)
  - **Verified fix:** a `PreToolUse` hook **is** invoked and **does** block — probe re-run with a side-effect canary (`touch <file>`): hook invocations 1, decision deny, **no file on disk**. ⚠️ Method note: the first version of the probe checked for the canary *string in the agent's reply* and reported "executed" — a false positive, because the agent naturally echoes the string when describing the command. A canary must assert a side effect, never text.
  - **Not yet fixed, because it collides with the spec — see the open question below.**
- **OPEN QUESTION FOR THE USER — the bash whitelist as specified would have blocked this ingest.** Analysis of all 68 Bash calls in the successful run: **54 are vault `scripts/*.sh`** (exactly what hard rule 4 permits) and **14 are not** — `find`, `ls`, `cat`, `echo … && …` chains, `python3 -c`/`-m json.tool`. So CLAUDE.md hard rule 4 ("bash restricted to a whitelist of the vault's `scripts/*.sh`") and the ingest skill's real behaviour are in direct conflict. Enforcing the rule as written is a live risk to the M0 deliverable, and this is exactly the "spec gap / wrong assumption about the ingest skill" case CLAUDE.md's working agreement says to stop and ask about. Options put to the user:
  1. **Enforce as specified.** The 14 exploratory calls have first-class tool equivalents (`Read`/`Glob`/`Grep`) the agent can use instead; deny the bash and let it adapt. Strictest, matches the rule, requires re-running the validation to prove the ingest still completes.
  2. **Allow read-only bash + vault scripts.** Keep writes and network out of bash; permit non-mutating exploration. Pragmatic, but "read-only bash" is hard to enforce reliably — deciding whether a shell string mutates anything is not tractable in general, so the guarantee would be weaker than it looks.
  3. **Amend hard rule 4** to reflect that the vault-write scoping (the part that actually protects the vault) is the load-bearing guarantee, and treat the bash restriction as defense in depth rather than a hard boundary.
  - **DECIDED (user, 2026-07-17): option 3.** Hard-enforced: writes confined to `VAULT_ROOT`, no web egress. Best-effort: the bash layer. CLAUDE.md hard rule 4 to be corrected accordingly. See the open gap below — the decision exposed that even the "hard" half is not hard for Bash.
- **RESOLVED — why the skill never loaded, and the fix (decision: fix now).** The vault ships `.claude-plugin/plugin.json` (`name: claude-obsidian`): **the vault IS a Claude Code plugin**, and its `skills/` directory is not a location the CLI scans on its own — Claude Code looks in `.claude/skills/`, user settings, or plugins. So `settingSources: ['project']` was never going to register `wiki-ingest`, and **SPEC.md §3.1 is wrong on this point** ("`settingSources: ['project']`, damit … die claude-obsidian-Skills geladen werden"). Measured before the fix: the agent's skill listing contained only the CLI's bundled skills (`deep-research`, `dataviz`, `code-review`, …) — not one vault skill. Fix: `plugins: [{ type: 'local', path: vaultRoot }]` + `skills: 'all'`. **Verified: the probe now reports `claude-obsidian:wiki-ingest` as invocable.** ⇒ SPEC.md §3.1 needs a correction; the ingest can now run through the skill deterministically instead of by improvising from `SKILL.md`.
- **RESOLVED — hard rule 4 is now genuinely enforced, by the OS.** The PreToolUse hook cannot cover Bash (deciding what an arbitrary shell string writes is not tractable, and the validated ingest needs general bash), so the tool-level guard is backed by `sandbox: { enabled: true, failIfUnavailable: true, allowUnsandboxedCommands: false, filesystem: { allowWrite: [vaultRoot] } }` — bubblewrap enforces it in the kernel. **Verified: the canary `touch /tmp/…` returns `Read-only file system`; probe reports `blocked`.** This is the first point in M0 at which rule 4 actually holds for Bash rather than being asserted.
  - **Dependencies: `bubblewrap` + `socat` only** (the SDK references exactly `bwrapPath` and `socatPath`; `xdg-dbus-proxy`/`slirp4netns` are not used). `bwrap` creates a sandbox successfully under WSL2 — user namespaces are available (`user.max_user_namespaces` = 126745), which was not a given. Both must go in the M5 setup script.
  - **`failIfUnavailable: true` verified working:** with `socat` missing the run aborted loudly — *"Sandbox required but unavailable: … socat not installed"* — instead of silently degrading to unsandboxed execution. That is exactly the desired failure mode.
  - **⚠️ THE IMPORTANT ONE — enabling the sandbox is NOT sufficient on its own.** The Bash tool ships a **`dangerouslyDisableSandbox`** parameter, and `sandbox.allowUnsandboxedCommands` **defaults to `true`**. Measured: with the sandbox enabled but that default left alone, the agent hit the write denial, set the parameter on its next attempt, and **created the canary outside the vault anyway** — reporting it plainly ("required disabling the sandbox override, since `/tmp` isn't in the default sandbox write-allowlist"). Setting `allowUnsandboxedCommands: false` makes the parameter ignored entirely. The PreToolUse hook additionally refuses it, so an attempt is visible in the log rather than silent. **A sandbox with an agent-accessible opt-out is decorative.**
  - Honest scope of the guarantee: writes are confined to `VAULT_ROOT` **plus a few allowlisted temp paths** the sandbox permits by default (`$TMPDIR`, e.g. `/tmp/claude`). Not the vault only — but nothing sensitive, and no longer "anywhere the process can write".
- **Bypass caught during the rewrite (mine):** the first version of the new bash policy short-circuited to "allowed" whenever the command contained a vault script — so `scripts/wiki-lock.sh list; curl https://evil.com` passed. A test caught it. The denylist now runs first, unconditionally. Being a vault script is never a licence to skip the checks.
- **Side effect of the plugin fix: the vault's hooks activated, and they commit.** Loading the vault as a plugin enables not only its skills but `hooks/hooks.json` — whose `PostToolUse` hook (matcher `Write|Edit`) runs `git commit -m "wiki: auto-commit $(date …)"`. Run 2 produced **13 such commits**. Run 1 produced none (no plugin ⇒ no hooks), which is why this only surfaced now. It breaks two spec guarantees at once: SPEC.md §3.1/§7 name the **service** as the single commit authority with message `ingest: <quelle>`, and SPEC.md §9's "Ingest rückgängig" button needs **one commit per ingest** to `git revert` — 13 granular commits make that undoable-in-principle only. **Decision (user): disable it via the vault's own documented opt-out** — the hook itself checks `[ -f .vault-meta/auto-commit.disabled ] && exit 0`, so this is a documented mechanism and stays inside CLAUDE.md hard rule 5 (no modification of repo internals). The 13 test-run commits were collapsed with `git reset --soft` into the single `ingest:` commit `313715e` (files untouched — verified 64 `.md` before and after).
  - ⚠️ Not yet verified at runtime: the opt-out flag is in place and the hook's own code reads it, but no ingest has run *since* it was created. Confirm on the next run that zero `wiki: auto-commit` commits appear.
  - **Follow-up for M1:** the service does not commit yet — both `ingest:` commits so far were made by hand. SPEC.md §3.1 wants the runner to commit after each successful job. Carry into TASKS-M1.
  - Also worth knowing: the vault's `SessionStart` hook `cat`s `wiki/hot.md` into every session, and its `Stop` hook nudges the agent to refresh the hot cache. Both are now active and are presumably why run 2 maintained `overview.md` and the hot cache more completely than run 1.
- Other:
- **Deviation from task 4.1 wording — "edit auto-accept" is NOT `permissionMode: 'acceptEdits'`:** the SDK's `acceptEdits` mode auto-accepts edits *wherever the process can write*; it has no path condition. Using it would satisfy "don't prompt" while quietly breaking CLAUDE.md hard rule 4 ("write access only under `VAULT_ROOT`"). The runner therefore uses `permissionMode: 'default'` plus a `canUseTool` callback as the single decision point: it scopes writes to the vault, enforces the bash whitelist, and denies web tools — deny-by-default, so a future SDK tool cannot silently inherit vault write access. `disallowedTools: ['WebSearch','WebFetch']` stays as defense in depth. Same intent as the task, enforceable rather than aspirational. **API shape verified against the installed SDK's `.d.ts`, not from memory** — the bundled `claude-api` skill explicitly does not cover the Agent SDK (it documents the separate Claude API / Tool Runner).
- **Bash whitelist is deny-by-default with no shell chaining:** `bash scripts/wiki-lock.sh list; rm -rf ~` starts with a whitelisted prefix, so a prefix check would pass it. Any command containing shell metacharacters (`;`, `&&`, `|`, `$()`, backticks, redirects, newlines) is rejected outright. Covered by tests.
- Live verification (no token needed): all four config-guard paths exercised through the real CLI — missing `VAULT_ROOT`, missing credential, **both credentials set (hard rule 3 refusal)**, and `VAULT_ROOT` pointing at the wrong directory. Each exits non-zero with an actionable message. `npm test` 66 passed, typecheck + lint clean.
- **Test-harness footgun worth remembering:** `beforeEach(() => queryMock.mockReset())` (expression-bodied arrow) returns the mock, and **vitest treats a `beforeEach` return value as a teardown callback** — so it called `queryMock()` with no arguments after every test, producing three bogus failures that looked like runner bugs. The production code was never wrong. Fix: `beforeEach(() => { queryMock.mockReset() })`. Isolating the behaviour in a scratch test (where it passed) is what separated "my test is broken" from "my code is broken".
- **Bug found by the smoke test — the credential never reached the agent (task 2.3):** `loadConfig()` held the token in a JS object, but the SDK **spawns a separate Claude Code process** that reads the credential from *its own* environment. Unauthenticated, that subprocess replies `"Not logged in · Please run /login"` — as a **`subtype: 'success'` result with `is_error: false` and zero tokens**. The runner therefore reported a no-op as a completed ingest. Two fixes, both covered by tests: (1) `buildAgentEnv()` sets `Options.env` — which **replaces** the child environment rather than merging, so `process.env` must be spread in explicitly — and strips both credential vars before re-adding only the configured one, so a stray `ANTHROPIC_API_KEY` in the service's environment cannot silently override the chosen token; (2) `runAgent()` treats a zero-token run as failed, because no tokens means the model was never reached regardless of what the SDK's subtype claims. **Lesson: do not trust `subtype: 'success'` alone — check that tokens were actually spent.** This would have surfaced during the PDF ingest as a confusing "ingest failed"; the isolated smoke test is what made it a five-minute diagnosis.
- **Credential leak (my error, 2026-07-17):** while inspecting `~/.config/vault-service/env` I used an "safe" inspector that masked values but printed **comment lines verbatim** — and the user's first token had been pasted onto the commented-out `ANTHROPIC_API_KEY=` line. The token was printed into the session transcript. It was never functional (commented out, and an OAuth token in the API-key variable), but it was a real credential. Action taken: env file reset, token removed from disk, user regenerated a new one. **Rule going forward: mask every line, including comments — an inspector that exempts any line class is not a masker.**
- Cost note: the smoke test's 6 tokens reported `$0.1413`. That figure is not proportional to the tokens shown and is only an API-price *estimate* in subscription mode anyway (SPEC.md §7.1 requires labelling it as such). Worth re-checking against a real ingest before the dashboard presents per-job cost in M3.
- Other:
