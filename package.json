{
  "name": "librisvault",
  "version": "0.1.0",
  "private": true,
  "license": "PolyForm-Noncommercial-1.0.0",
  "author": "Benjamin Steinborn",
  "repository": {
    "type": "git",
    "url": "https://github.com/steinborndev/LibrisVault.git"
  },
  "type": "module",
  "description": "Local ingestion service + dashboard on top of a claude-obsidian vault",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web",
    "lint": "npm run lint --workspace server && npm run lint --workspace web",
    "ingest": "npm run ingest --workspace server --",
    "permprobe": "npm run permprobe --workspace server",
    "preprocprobe": "npm run preprocprobe --workspace server",
    "vaultprobe": "npm run vaultprobe --workspace server --",
    "vaultrepair": "npm run vaultrepair --workspace server --",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "build:web": "npm run build --workspace web",
    "build:server": "npm run build --workspace server",
    "build": "npm run build:web && npm run build:server",
    "start": "npm run start --workspace server",
    "start:prod": "npm run start:prod --workspace server"
  }
}
